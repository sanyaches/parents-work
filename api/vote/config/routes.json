{
  "routes": [
    {
      "method": "GET",
      "path": "/votes",
      "handler": "Vote.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/votes/count",
      "handler": "Vote.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/votes/:id",
      "handler": "Vote.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/votes",
      "handler": "Vote.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/votes/:id",
      "handler": "Vote.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/votes/:id",
      "handler": "Vote.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
