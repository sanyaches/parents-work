{
  "routes": [
    {
      "method": "GET",
      "path": "/chats",
      "handler": "Chat.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/chats/count",
      "handler": "Chat.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/chats/:id",
      "handler": "Chat.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/chats",
      "handler": "Chat.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/chats/:id",
      "handler": "Chat.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/chats/:id",
      "handler": "Chat.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
